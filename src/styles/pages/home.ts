import { styled } from "..";

export const HomeContainer = styled('main', {
  display: 'flex',
  maxWidth: 'calc(100vw - ((100vw - 1180px) / 2))',
  minHeight: 656,
  width: '100%',
  marginInlineStart: 'auto',
})
